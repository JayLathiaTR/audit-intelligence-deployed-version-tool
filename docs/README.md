# GitHub Pages

This repository is primarily a Windows desktop tool.

GitHub Pages is not used for the application UI because the target APIs do not allow browser cross-origin requests (CORS) from `*.github.io`.
